# METABEGIN
# X-Env-Layer-Name: mp-suite-trixie
# X-Env-Layer-Category: suite
# X-Env-Layer-Desc: Meltingplot base suite. Debian Trixie with apt, utils, wired
#  networking and ssh. This is trixie-minbase without wifi
#  (wireless-regulatory, iwd): the printer SBC is wired to the HMI on a
#  dedicated VLAN and has its radios disabled by mp-net-static. Also without
#  rpi-misc-skel, whose only content is an /etc/hosts that pins the build time
#  hostname; mp-identity ships its own, since the hostname is set at runtime.
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: debian-trixie-arm64-multi,
#  rpi-debian-trixie,
#  rpi-misc-utils,
#  rpi-essential-base,
#  systemd-net-min,
#  openssh-server,
#  systemd-timesyncd
# METAEND
---
